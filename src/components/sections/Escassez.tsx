import { component$ } from '@builder.io/qwik';
import Section from '../Section';

export default component$(() => (
  <Section title="8. Coloque escassez" id="escassez">
    <p class="lead">Estamos liberando as demonstrações de forma limitada para garantir um atendimento personalizado a cada loja. — vagas restantes: <span class="badge">5 por estado para essa semana</span>. Garanta já sua vaga.</p>
  </Section>
));
