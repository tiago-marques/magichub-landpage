import { component$ } from '@builder.io/qwik';
import Section from '../Section';

export default component$(() => (
  <Section id="garantia" fullScreen>
    <p class="lead">A demonstração é 100% gratuita. Você só avança se fizer sentido para sua loja.</p>
    <p class="section-content">Sem contratos. Sem obrigação.</p>
  </Section>
));
