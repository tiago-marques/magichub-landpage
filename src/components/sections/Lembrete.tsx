import { component$ } from '@builder.io/qwik';
import Section from '../Section';

export default component$(() => (
  <Section title="12. Lembrete" id="lembrete">
    <p class="lead">Sua loja já vende bem. Agora é hora de aparecer no digital também. Comece sem complicação.</p>
  </Section>
));
