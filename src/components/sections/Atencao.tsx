import { component$ } from '@builder.io/qwik';
import Section from '../Section';

export default component$(() => (
  <Section id="atencao" fullScreen>
    <div class="w-full text-center">
      <p class="lead text-2xl sm:text-3xl md:text-4xl"><strong>Leve sua loja física para o digital sem complicação</strong></p>
      <p class="section-content mt-4 max-w-3xl mx-auto">Transforme suas notas fiscais (NFe) em um catálogo online automaticamente — sem precisar entender de tecnologia.</p>
    </div>
  </Section>
));
