import { component$ } from '@builder.io/qwik';
import Section from '../Section';

export default component$(() => (
  <Section title="1. Chame a atenção" id="atencao">
    <p class="lead"><strong>Leve sua loja física para o digital sem complicação</strong></p>
    <p class="section-content">Transforme suas notas fiscais (NFe) em um catálogo online automaticamente — sem precisar entender de tecnologia.</p>
  </Section>
));
